[package]
name = "agni-core"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
spirit-sdk = { path = "../../../spirit/spirit/sdk" }
