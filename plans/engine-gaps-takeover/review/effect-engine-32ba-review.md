# Engine 32ba0dbe — bounded follow-up

Read only the committed `sim/src/log.rs` delta from merged parent cd4ef497, against the 8e1 review and frozen Peek addendum. No builds or edits. **Three prior source findings are corrected; the effective-seat admission finding remains partially open.** SDK source remains separately blocked by the 0be7 review.

## Corrected source paths

- `rollback_effect_group` performs the final pending-request face/revealed/shown cleanup before pruning Peeks. This now follows the exact final-audience addendum, including Owner seat and shown handling. The new `rollback_prunes_peeks_after_pending_reveal_reconciliation` exercises a real public reveal, hidden round trip, accepted later Peek, new owed request, rollback, and viewer1 face visibility; it would fail with the old ordering.
- `raw_annotation_limits` reads and caps the outer card-row map length before `map_spans` allocates its entries, then reads each row's advertised size and remaining aggregate limit before allocating row entries/keys. This closes the prior specific metadata-allocation hole for the capped annotation collections.
- `raw_current_limits` now checks recognized active current cards/counters/annotations/disclosures before `cbor_value`; optional omitted fields remain allowed. The immutable roster's advertised count is capped at 256 before deriving Peek capacity. Nongroup ordinary collections still bypass feature caps; the old >16384-annotation and zero-span/zero-step controls remain intact.

## Remaining source blocker

**P1 — effective membership is checked only for plugin effects.** New checks in `apply_effect` Move/Spawn correctly normalize Shared before checking destination membership, and validate Spawn owner before allocation. However `effective_seat` itself still only normalizes; `validate_action` Move (around line516) and Spawn (around line627) still accept a PerSeat+All destination addressed to an absent ID. With roster [0,7], seated actor0 can submit ordinary Move or Spawn to a declared PerSeat+All zone with seat1. The action passes validation and `fold_begin` writes Card.seat=1; the resulting snapshot fails `validate_table` during restore. This is the same admitted-state/restore invariant hole reported previously, via the other callers.

Use the common `effective_seat` helper to normalize then reject an unseated effective ID, or add equivalent validation to both incoming action paths. Shared supplied seat1 must remain valid at effective seat0; no change to existing foreign-private-zone guards. The current new test covers plugin effects only and therefore cannot detect this residual path.

## Required acceptance gaps, separate from source findings

- Add incoming Move and Spawn controls on sparse [0,7]: PerSeat7 succeeds; PerSeat1 returns UnknownSeat before mutation; Shared input1 succeeds with stored seat0. Compare complete state/cursor before and after rejection; save/reload accepted results. Existing effect test already discriminates absent owner and unchanged allocator, but should also assert successful Shared placement/owner values and cover absent PerSeat Spawn destination.
- The commit adds **no new raw-bound fixtures**. `snapshot_bounds_run_before_recursive_decode_and_semantics_are_checked` still starts with a truncated, wrong-shaped `effect_group` array. Its None result proves neither collection-specific preallocation guard nor the new valid controls. Add complete correctly shaped active snapshots at/beyond the card, counter, annotation aggregate and actual-roster Peek limits, in capsule and current state. Assert the raw prepass rejects the oversized field before Value decoding (a direct private helper assertion or test-only decode-phase sentinel suffices); assert valid shape and exact-cap controls decode. Include several individually small annotation rows whose sum exceeds the aggregate, and an overlarge outer annotation map. Keep a nongroup equivalent over feature limits accepted.
- The new pending-Peek test checks viewer1 only and does not round-trip the final snapshot. The planned cross-layer matrix should additionally assert owed/revealed/shown, Owner viewer0 and unrelated viewer behavior, and fresh engine/SDK reconstruction. This is an acceptance gap rather than another source defect in the corrected reconciliation loop.
- ABI0/lifecycle, broader mechanics, projection equality and native/hardened HostSession tests remain assigned to the shared integration gate. Reported core/sim/host/golden/wasm/clippy/fmt passes are not reproduced in this source review and do not substitute for the missing discriminating vectors.
