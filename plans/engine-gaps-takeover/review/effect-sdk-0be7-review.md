# SDK checkpoint 0be7cd15 — source disposition

Reviewed committed SDK/game delta only against engine 8e1ebe62 and the frozen protocol, roster review, and Peek authorization addendum. Baseline merge cd4ef497 has no manual changes and does not alter this disposition. No builds or production edits. **Not ready for cross-layer acceptance: the bounded blockers below remain.** Paths below are relative to agni.

## Repairs credited

- `plugins/sdk/src/decide.rs:parse` requires all six ABI1 keys, protocol exactly 1, no trailing data/duplicate top-level keys, and now invokes semantic projection validation. Disclosures and `despawn_any` come from the authoritative state, not a top-level override.
- Immutable `despawn_any` is copied into `EffectProjection`, outside the rollback capsule. Its focused true/false non-token test discriminates the capability.
- Roster parsing preserves actual u8 IDs/order, rejects duplicate IDs and advertised >256 seats before allocation, and exposes an exact u16 count. Sparse and 256-seat parser tests are useful; they do not yet prove admission/projection parity.
- Current Reveal/Peek bookkeeping, accepted allocator high-water preservation, cloned effect/entry atomicity, and active first-marker checking improve the earlier checkpoint. They do not close the remaining sequences below.

## Must fix

1. **P1: disclosure projection differs from the engine on ordinary mutations.** `effect_group.rs:apply_effect_inner` sends Move only through Snapshot, missing removal of `shown` on actual relocation and complete disclosure cleanup when a token disappears on a shedding move. `apply_action_disclosures` erases Peek for every shedding destination, whereas engine shedding forgets faces only if it destroys a token; `hidden=true` fails to remove owed reveals. Clear gathers removed IDs after `table.apply_entry` already deleted them, so it removes no disclosure rows. Capture pre-mutation identity/token facts and mirror engine's exact successful mutation behavior, including no-op Effect::Move. Vectors: shown Owner card moves to another Owner seat; non-token Peek survives ordinary shedding; token shedding removes all disclosure; hidden incoming Move clears owed; Clear removes all three disclosure sets for swept cards. Compare whole projection with authoritative post-entry request, with and without a group where legal.

2. **P1: rollback still mishandles ordered Reveal debts and exact Peek reconciliation.** `effect_group.rs:432–520` checks a transient RequestReveal for an *earlier* PublicReveal, rejecting the legal transient Request→Public→Despawn→Rollback sequence. For restored live cards it leaves a later outstanding request simultaneously reconciled/revealed/shown instead of blanking the face and clearing revealed/shown after the ordered pending pass. Reconcile an idempotent ordered pending set, then enforce live/revealable unresolved obligations, then prune redundant Peeks using the frozen final-audience rule. Current pruning also fails to remove the restored Owner's own redundant Peek when `shown` is false. Required vectors: coalesced requests fulfilled once; unsolicited public reveal followed by a fresh hidden/revealable request remains owed; transient fulfilled request disappears on rollback; latest live debt clears face/revealed/shown; baseline/later Peek for viewer1 survives Owner restoration; same-Owner public reveal followed by later pending debt retains necessary viewer1 Peek; redundant Owner-viewer0 pair is removed. Assert sets and actual two-seat engine views after reload.

3. **P1: lifecycle and limit guards can be bypassed.** `is_membership` excludes Begin, allowing an entry that starts active to Commit/Rollback(old)→Begin(next). The test named `ordinary_effects_after_commit_are_allowed_but_another_lifecycle_is_not` never attempts that forbidden second lifecycle. Active per-effect mechanical/Peek limits are checked only by `finish_entry`; Begin at 4096 cards→Spawn #4097→Commit is accepted by SDK because terminal removal bypasses the final active limit. Apply group limits after every successful ordinary effect, atomically, as the engine does. Include both incremental API and `project_verdict`, terminal→Begin/Continue/Commit/Rollback, valid terminal→ordinary suffix, and exact-cap/control/over-cap→terminal sequences.

4. **P1: OutstandingDisclosure is enforced at the wrong boundaries.** `begin_entry_inner:181` preserves outstanding obligations even when no group exists, rejecting ordinary Game on an otherwise valid nongroup owed-hidden state. Conversely `begin_group` validates only size, not Outstanding: Begin with owed-hidden→Move to All→Commit can pass SDK though engine rejects Begin. Gate preservation on an already active group; validate outstanding at Begin before capturing a capsule. Tests must contrast nongroup Game/Move-to-All acceptance with Begin refusal, and assert failed Begin does not consume ID or change state. Guard alone does not implement the future cost reveal cursor.

5. **P1: semantic/parser validation remains incomplete.** `from_decide` never compares before.next_id <= current.next_id or validates ordered pending information against current state. `validate_information` accepts hidden PublicReveal faces and PublicReveal in a None zone; transient IDs below before.next_id but absent from the baseline also pass. `validate_disclosure_shape` accepts revealed-in-None and shown outside Owner. `validate_snapshot` does not validate duplicate zone/counter declarations, counter min/max/value bounds, or directly constructed roster/count consistency. Current annotations silently discard unknown card rows and overwrite duplicate outer card keys (`table.rs:parse_state_with_disclosures/annotated`); current counters accept repeated/missing fields. Current `zone_id` maps an unknown tagged zone or out-of-range Plugin ID to Hand. Close these concrete invalid-input paths with independent complete CBOR fixtures, retaining valid transient allocation/history and revealed+necessary-Peek Owner controls. Strict parsing must not silently normalize malformed journals/state into valid projections.

6. **P1: active request limits are still checked after allocation.** Current-state cards/counters/annotations/disclosures are parsed without active-group caps; `parse_mechanical` permits MAX_PEEKS=4096*256 before the actual roster-derived cap is checked. Arbitrary top-level field order means a bounded first pass (or equivalent borrowed-span validation) must discover active group and immutable roster before allocating those collections. Capsule annotation aggregates themselves are now bounded before row allocation, which is a real improvement. Add complete valid-shape boundary/over-limit current and capsule fixtures in both key orders, including a small roster's Peek cap. Keep these feature caps scoped to active group requests; do not impose them on valid ordinary nongroup states.

7. **P1: roster membership and game adapter boundaries are incomplete.** SDK `table.rs:move_card/spawn` skips per-seat destination validation whenever visibility is All. This permits an invalid absent-seat card just like the engine bug already reported; a later from_decide rejects it. Normalize Shared to effective seat0 first, then validate effective membership; validate explicit/default Spawn owner separately before allocation. Test sparse [0,7]: PerSeat7 accepted, PerSeat1 rejected, Shared supplied1 accepted at0, owner1 rejected even Shared, failure atomicity. This is a coordinated engine/SDK admission repair, not a contract weakening.

   Game-side `lib.rs:438` uses saturating Join count without validating proposed seat: dense [0]→Join7 passes and makes the next request unsupported. Enforced early delegation and public `engine::decide/start` bypass the top-level check; engine::decide returns acceptance for Join directly. Add the same checked game boundary before either entry path projects/mutates: supported dense current roster; Join actor equals next dense ID; checked increment rejects 255→256. `Ctx::players` must not substitute blob count for unsupported rosters. `present::contested` also fabricates a one-player result on unsupported direct input; return an empty/unsupported result consistently or require checked context explicitly. For the changed dense-table/blob mismatch, preserve the previous max policy after validating the actual roster, or explicitly refuse an incompatible persisted count; do not silently shrink iteration while the blob retains a larger turn order. Test direct engine entry, top-level native/bytes entry, passive present and dense 2/3/4 controls. Neutral 256-seat support does not imply Riftbound support.

## Acceptance still needed after source repair

- Run the shared engine/SDK differential vectors through authoritative DecideRequest/from_decide. Include real current-state disclosures and immutable despawn_any, not only hand-built Request values.
- The focused SDK tests cover a simple requested reveal, allocator rollback, capability true/false, wrong-ID atomicity, and roster parsing. They do not establish the multi-request disclosure, malformed-state, cap, or post-terminal claims above. In particular the named post-terminal test currently asserts only group_id=None.
- Full ABI1 consumer gates are required: SDK, Riftbound library + independent blob/match fixtures, MTG, plugin builds, and Kai adapters. Updating the nine direct Request literals and old three-key builders is necessary; targeted fixture pass does not replace the complete merged gate.
- Fresh hardened engine/plugin artifacts and the planned neutral HostSession/native/wasm matrix remain required after both source halves pass. These gates are separate from card-specific Costs4 continuation work, which remains unimplemented.

Root's subsequent gate update: merged Riftbound passed 4494 library +10 blob +10 match tests, and workspace --all-targets --no-run passed. This closes the current cross-crate compilation/full game fixture gate; it does not close the SDK runtime/protocol vectors or source blockers above.
