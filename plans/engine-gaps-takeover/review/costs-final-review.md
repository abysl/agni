# Final Costs three-cluster review

Reviewed `/tmp/agni-takeover-costs` HEAD `95327414` against the earlier reviewed `bfe8bbc8`, including the actor plumbing, Play test overlay, and final corrections. Source/test inspection only; no builds or production edits. The working tree was clean.

**Disposition: bounded approval. Both original must-fixes and the two intervening corrections are closed. No remaining production blocker was found in this follow-up delta.** Root's final fresh-artifact parity/Kai gates remain integration conditions. Fourth additional-cost settlement (`d0639152`) is not ported or accepted by this review.

- **Legend eligibility:** `engine/ctx.rs::face_in_play` now requires a declared Legend zone and equality with `Some(legend)` for the Legend exception. The `is_some_and` guard excludes the previous `None == None` mistake. Tests cover a Legend in its proper zone, the same kind in Hand/Trash, removal of an existing empowerment counter off-zone, and absent Legend-zone declaration/placement. This closes the counter-write regression; the broader logical-zone/object-incarnation source guard remains A1 work. The board arm of the predicate is intentionally unchanged.
- **Disempower payment:** `engine/activate.rs::pay_self` checks empowered, in-play and ready before exhausting, then checks the disempower result. These checks cover the current counter setter's ordinary failure preconditions; no readiness-suppression check blocks exhausting. `a_ready_suppressed_source_can_still_exhaust_to_pay_disempower` explicitly verifies that distinction. Existing Lantern tests cover unempowered/exhausted refusal, actual payment before chain resolution, no early draw, and inability to pay again. These establish validation before ordinary refusal, not general cross-request compensation/A5 rollback.
- **Item actor propagation:** prelude Empower, Ambessa's shared helper, Kayle's first and later counters, Mel Defiant Soul, Escaped Grayback, Punching Poro, Mournful Witness, Renekton, and Wild Claw now pass `item.controller`. Time Warp and Arcane Shift self-banish also pass it. The remaining Kayle/Wild Claw convenience wrappers derive current controller only for their existing non-item/test callers; no production item caller still uses those wrappers. This closes the concrete omissions from the original review without changing the default helper to the unrelated request actor.
- **Actor regressions:** the new Ambessa-module test creates a real Questionable Tome activation, preserves its queued item through GameBlob encode/decode and table reconstruction, changes the source's controller, resolves the chain, and checks seat-0 Empowered plus positive/negative YouEmpower matching. Time Warp likewise starts a real play and retains the queued item through reconstruction, then verifies extra-turn seat and Banished's separate actor/owner/token fields despite changed current control/owner. The ownership mutation is deliberate fixture setup, not a claim that ordinary game actions can change physical ownership. These are native fixture continuations with serialized plugin blobs and cloned tables, not full engine-snapshot or network-replay tests; root's parity gate supplies separate integration evidence.
- **Play overlay:** `engine/play.rs` changes in this follow-up are tests only. The paired Limited regressions preserve a captured external destination after its grant source leaves, and reject a newly prohibited destination before paying runes/Gold. The second deliberately places the pending item at STAGE_PAY and mutates the Warden into place to isolate the final boundary. It is not an independently admitted network mutation sequence and does not establish rollback after payment.

No new blob/wire schema, cost interner, slot reassignment, or additional-cost stage appears in this correction delta. The prior bounded acceptance of the three-cluster actor/token matching and merged Play/Economy/A9 behavior stands. A1 incarnation validation, A3 persisted event capture, D1 replacement-source attribution, and the fourth cluster's complete payment cursor/neutral mechanical undo remain separate dependencies, not newly closed features.

Reported by root: 4479 library passed / 222 ignored, 10 compatibility and 10 MatchState passed; clippy/fmt passed and fresh wasm built. I did not reproduce those gates. Accept integration after the remaining fresh-artifact parity/Kai checks succeed; no extra production correction is requested by this review.
