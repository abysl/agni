# Inventory unknown dispositions at Economy checkpoint

Source inspection at e9235605 found ten `unknown` entries. All ten have successfully parsed test attributes and explicit ignore reasons; they are classified unknown because their prose does not begin with the audit's exact `engine gap` prefix. This is not a Rust parsing failure or evidence of coverage. Keep them unresolved until their assigned lane or batch enables and verifies the behavior. No source ignores, immutable inventory entries, mappings, or audit policy were changed.

- triggers / trigger-subject-widening: `back_alley_bar.rs::an_enemy_unit_moved_from_here_by_an_effect_gets_the_bonus_too` — Trigger::Move reads Who::You as the holder's units; the bar says any unit: the engine owes Who::Any for Move (and Attacks) so an enemy unit moved from here by an effect gets +1 too
- triggers / trigger-subject-widening: `the_dreaming_tree.rs::the_opponent_choosing_their_own_unit_here_with_a_spell_draws_too` — Trigger::ChosenFriendly reads the chooser as the holder; the tree says any player: the engine owes Who::Any on ChosenFriendly so the opponent choosing their own unit here with a spell draws too
- triggers / trigger-subject-widening: `pirates_haven.rs::readying_a_friendly_unit_gives_it_one_might_this_turn` — Trigger::Readied matches only the source itself (triggers::matches); a Readied(Who::Friendly) that hands the readied unit over as the subject is an engine gap
- triggers / died-killer: `solari_shrine.rs::killing_a_stunned_enemy_unit_asks_to_exhaust_the_shrine_and_draws_one` — Event::Died carries no killer and Noted no stunned flag (kill.rs drops the card state before the event); the shrine needs both to know you killed a stunned enemy
- costs / base-cost-discard: `unlicensed_armory.rs::the_discard_is_paid_before_the_ability_reaches_the_chain_and_the_fury_is_asked_for` — the discard is a base cost paid at the pay stage (204.1.b) and the Fury payment is a may on the kill path, which has no prompt; both are the non-resource-cost and kill-path-prompt engine gaps
- play / origin-revealed: `dazzling_aurora.rs::the_played_unit_reports_an_origin_that_is_not_banishment` — play::begin knows no origin for a card revealed off the deck, so the free play rides Origin::Banishment and Event::Played misreports where the unit came from; an Origin for a revealed deck card is an engine gap
- economy / tags-on-the-face: `gentle_gemdragon.rs::a_dragon_tagged_unit_under_a_name_the_lists_do_not_know_still_counts` — CardInfo carries no tags: is_dragon reads the printed name over herald_of_scales::DRAGONS, so a Dragon-tagged unit under a name the list does not know is invisible; the engine owes a tags row on the face and Filter::Tag
- economy / tags-on-the-face: `poro_herder.rs::a_poro_tagged_unit_whose_name_lacks_the_word_still_counts` — CardInfo carries no tags: is_poro reads the printed name, which every Poro so far carries; a Poro-tagged unit named otherwise needs a tag on the face
- economy / tags-on-the-face: `starhound.rs::a_dog_tagged_unit_under_a_name_the_list_does_not_know_still_counts` — CardInfo carries no tags: is_companion reads the printed name over the Bird, Cat, Dog and Poro lists of every set, so a unit tagged under a name the list does not know is invisible; the engine owes a tags row on the face and Filter::Tag
- damage / bonus-damage: `ravenborn_tome.rs::the_next_spell_this_turn_deals_one_more_and_the_one_after_does_not` — Bonus Damage keyed on the cause in Ctx::damage and a per-seat next-spell promise for the turn are engine gaps (417.4, 3650)
