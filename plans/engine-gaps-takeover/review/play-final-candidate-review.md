# Play final candidate: bounded independent review

Candidate: 7b94eec0 in `/tmp/agni-takeover-play`, relative to e9235605. Read-only source/test review of the five previously reported blockers. No builds or production edits. The only uncommitted delta observed is the helper's test-module correction in `engine/cost.rs`; it restores a custom test script after fixture reconstruction and asserts actual draw events.

**Disposition: no remaining production must-fix found within these five blockers. Bounded acceptance is conditional on the final test patch and fresh integration gates.** One previously requested mutation-boundary regression remains unverified in the inspected source (below); do not represent that specific acceptance item as tested.

## Findings against the five blockers

1. **Actual incoming Move and location grants/restrictions — source correction accepted.** `engine/mod.rs` marks the incoming projected card during `legal::classify`; `Ctx::units_at` excludes it from occupancy checks, then ordinary act clears the marker. `legal::play_to` applies own-only restrictions at base and battlefield, base-only restrictions, grants and the common no-unit-play restriction. The real `engine::decide` tests in `engine/legal.rs` cover Sneaky Deckhand open fields, Dauntless Vanguard enemy fields, forbidden destinations and matching view rows. The external Miss Fortune test uses an actual Move and verifies arrival's exhausted annotation. These are stronger than the former hand-built `EntryMove` classification-only checks.

   Limited destinations retain their captured allowed zone list while `limited_locations` rechecks `Ctx::limited_play_locations`, which intersects current base-only/no-play/own-only restrictions. `location_still_open` repeats that check for the selected destination at payment. The former unconditional Limited success is gone. **Remaining test limit:** I did not locate the requested paired saved-continuation regression in which a captured permission survives its grant source leaving, while a newly introduced Warden/base-only restriction prevents a queued child without payment. Add/identify that bounded pair before claiming the mutable-restriction acceptance fully covered. Do not remove these current checks to satisfy the old Harrowing fixture; the candidate now repairs that fixture with real occupied control. E1 historical-Conquer debt remains separate.

2. **Item-qualified Accelerate, LessEnergy, and Ignored — source correction accepted.** `origin_cost` uses the complete item for Accelerate eligibility, including non-Limited Revealed origins. LessEnergy is applied after optional additions/surcharges rather than exhausting its discount against printed base first. `of_item` applies the final Ignored override after complete calculation, clears all physical resource components, and preserves matching promise metadata. `begin_limited` no longer auto-declines optional slots. Bone Skewer has accepted/declined Ignored Accelerate reload tests with zero-resource and entry/stun outcomes. The cost tests cover qualified Accelerate and LessEnergy crossing the optional-cost boundary. Ignored Repeat and Clockwork Keeper accepted/declined reload tests exercise the corrected optional-choice semantics; the observed helper patch fixes the test registry after reconstruction and strengthens the Repeat outcome assertion. Gate the final patched tests, not a preceding test report.

   No A9 global interner or pointer-backed cost ownership was restored. Economy's additional/promised Repeat/mode slots remain intact. Non-standard costs are still the separate next Costs lane; these tests do not claim that future settlement pipeline exists.

3. **Swarm Queen legacy stage and current quotation — accepted within scope.** `LOCATE = 4` now retrieves the selected card from the saved item, validates the selected location, queues exactly that card and recycles the rest. `a_saved_legacy_location_resume_plays_the_selected_card_and_recycles_the_rest` constructs v11 bytes, reconstructs the saved table/blob, answers BF1 and asserts SECOND's destination, TOP's deck order and both reveal flags cleared. Its successful outcome cannot be obtained merely by restarting deck confirmation. Current revealed quotation uses `Origin::Revealed` plus `Price::Printed`, `cost::of_item`, item-aware affordability and the first target-spec fillability check. It no longer pays the raw printed helper before finalization.

4. **Revealed origin, viability and cancellation — accepted within scope.** Revealed plays carry the explicit Deck origin; complete-item helpers preserve source qualifiers and Limited price rather than impersonating a Hand play. Owner-facing `viable` greying remains separate from authoritative candidate membership; the prompt test explicitly retains a greyed option. Failed Revealed plays return to the physical owner's deck through cancellation. Existing Swarm/Promising Future fixtures exercise unpayable or untargetable revealed cards and their return/recycle outcome. First-spec fillability is exactly that local check, not proof that every future multi-target declaration is satisfiable; the review does not claim a complete target-search foundation.

5. **Child declaration, parent Repeat and legacy stages — accepted for the recovered Limited path.** `chain::run` marks callback execution as deferred; `begin_limited` moves/enqueues a Pending child without advancing choices. `chain::proceed` checks a resolving parent before servicing queued children. Ordinary parent completion/Repeat therefore runs before child location/payment choices, with normal child spell priority afterward. The repeated Here to Help regression reconstructs at face waits and later location prompts; it asserts the first child is still pending and unpaid while the parent requests its second pick, then verifies both final destinations. This tests the corrected 354.3/158.3 ordering rather than the abandoned “resolve child then resume parent” design.

   Current Here to Help queues the full permitted destination set; LOCATED is legacy-only. Its v11 continuation test verifies the remembered selected unit and destination. Promising Future retains a real LOCATE handler; its v11 gameplay test saves a chosen opponent card, answers the battlefield and checks one play plus payment and parent cleanup. Dazzling Aurora's card-owned PLACE 255 is unchanged; there is no global CHILD_WAIT 255 interpretation or Flow::Child variant. Its origin fix does not renumber legacy stages. The legacy Dazzling `play::begin` path itself was not comprehensively migrated to the new Limited architecture; this is not a claim that every existing synchronous play helper now obeys the new child protocol.

## Schema and gate limits

The writer is v12 with an explicit 14-field ChainItem and final Limited field. v7/v9/v10/v11 readers retain the 13-field shape; mode-slot insertion only occurs before v11. Owned CostedGrant encoding is preserved. Independent compatibility fixtures cover legacy chain and Pending rows plus wrong arity. Structural decoding remains distinct from the later A1/A4a legacy-admission policy.

Main's reported 4452 library / 236 ignored, 10 compatibility, 10 integration, clippy and formatting results are not independently rerun here. Parent owns fresh native/hardened parity, Kai and final merged-tree/artifact checks. The helper's final cost-test-only commit still needs its focused gate. None of these results establish universal gas/allocation equality or close A1–A12/E1 debt.

Before final integration: include the helper's test correction, resolve/document the paired captured-permission/current-prohibition test acceptance, and require parent's fresh final gates. No additional production redesign is requested by this review.
