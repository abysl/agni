# Triggers checkpoint a2f0d87d

The repaired Triggers source is checkpointed at a2f0d87d, with the strict-lint-only test correction at 40861960. The full Riftbound package passes 4533 tests with 177 ignored. The independent blob compatibility suite passes all 10 vectors after its canonical migrated snapshots were advanced to the frozen v14 schema. Warnings-denied clippy passes for all Riftbound-turns targets after the assertion lint correction.

The source changes close the bounded review findings: trigger batches are ordered conservatively, excess damage keeps the explicit damage assigner, copied Lent activations retain complete-item Empower discounts, and excess rows are sorted and duplicate-rejecting on decode. A registered copied Empower regression and the real trigger-order prompt controls are present.

This is a source and package checkpoint only. Fresh native/hardened replay, the independent v12/v13/v14 reload corpus, and the final cross-lane Kai gate have not run. The candidate remains separate from integration until those checks and a final source review complete.
