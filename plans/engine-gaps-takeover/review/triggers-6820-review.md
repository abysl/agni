# Triggers recovery 6820e6dd — bounded source review

Compared 6820e6dd with Damage baseline 68ce096f, saved 7dfaef5f and triggers-current-port-review.md. No builds or production edits. **Do not accept this candidate yet: three production corrections and the migration/reload evidence below remain.** Later A3/A4a/D1/A11 work is not part of this disposition.

## Production corrections

1. **P1: prohibited pointer-based ordering elision was imported.** `engine/triggers.rs:interchangeable` now compares Ability pointers rather than script+index, newly admits Granted items to the optimization, and ignores holder/lender/kind. That broadens the previous flawed optimization exactly where the checklist required conservative ordering. Return false for multi-item batches unless a separately justified semantic equivalence is available; the bounded recovery needs no A4 redesign. Add a two-holder or distinct-lender same-callback batch that must offer OrderTriggers. Update the copied-Blitzcrank Svellsongur test's explicit “two copies ... need no ordering” assertion; it currently entrenches the prohibited behavior.

2. **P1: excess loses the actual damage assigner.** `combat::deal` takes showdown before calling new `record_excess`; its loop still calls ambient `lethal(ctx, unit)`. `needed` reads `assigner(ctx)`, so Elder Dragon's one-damage-lethal threshold disappears and excess is understated. Thread the loop's explicit assigner through required-assignment calculation; keep finite prevention/multiplier semantics and separate attacker/defender records. Required real assignment tests: Elder Dragon against a higher-Might defender, prevention+Lotus multiplier threshold, and both sides producing distinct excess. None of those was added in this candidate; manual `record_excess_damage` card tests do not validate combat calculation. This is a known port interaction, not A11 replacement-order implementation.

3. **P1: Lent activations lose complete-item ability discounts.** `cost::discounts_of` returns free for Lent and `ability_discounts` rejects it; `risen_altar::is_empower_activation` also matches only Ability. Concrete registered combination: Tail-Cloaked Matriarch wearing Svellsongur at its controller's Risen Altar has a copied Empower activation represented as Lent, whose holder still has Empower but receives no reduction. Treat Lent as an activation throughout complete-item eligibility and retain holder/controller semantics. Compare printed and copied Empower with the same chosen energy/power reduction through admission, payment and reload. Do not simply discount every Granted trigger. Gear-activation counting in `play::finalize` correctly uses the holder and is preserved.

4. **P2: `xd` does not yet satisfy the frozen canonical record contract.** `record_excess` appends unsorted records and updates only the first match; decoder accepts duplicate seat/zone rows, with first-wins lookup and unchanged duplicate re-encoding. Canonicalize by (seat,zone) at the defined storage/decode boundary and reject ambiguous duplicate inputs (or freeze one explicit normalization policy consistently); preserve Some(0) versus absence, replacement, expiration and combat-end survival. Add independent bytes and order/duplicate controls. Existing live roundtrip covers replacement only in insertion order.

## Schema disposition and precise missing evidence

- v14 is correctly reserved. SeatState16/CardState15/ChainItem14 and Limited layouts are not changed. Noted uses exact old4/new5 arities through versioned ChainItem decoding, including Pending's nested item; old versions reject tags4/5, new Granted/Lent write four fields and existing tags remain three. A9 owned CostedGrant and Damage v13 fields remain in the source.
- The only new legacy fixture is `v13_noted_rows_keep_the_old_four_fields_and_reject_new_item_kinds`: one minimal v13 chain Spell, nonzero Noted and old Granted rejection. It is independently laid out rather than a version-byte trick, but does not cover the required v12/v13 pending path, Limited, selected mode/Repeat, awaited/remembered targets, nonzero seat promises/pool and owned grants. Existing independent blob/match fixtures remain unchanged; supplement them, do not replace them with current writers.
- Add an independently authored v14 expected encoding covering Granted and Lent in both chain and Pending, Noted.buffed=true, and nonzero xd; reject wrong kind/Noted arities and legacy new tags. New self-encode/decode assertions alone cannot prove exact wire compatibility.
- Forge changed-holder, Gardens departed-holder, Heimerdinger departed-lender, borrowed-granted exhaust, Warmog detach/dead-holder and copied-Blitz tests all remain present and active. They mostly mutate one live Ctx and resolve immediately; strengthen the saved-item cases with table+blob reconstruction and a fresh Resolved registry before the subsequent native request. Preserve both holder and lender IDs, cost/source/controller, stage/targets and awaited fields. Root is independently checking these fixtures.
- `effect_move_records_the_resolving_item_controller` demonstrates the helper choosing a manually installed resolving item over actor. Add an actual queued movement ability resolved by the opponent's final Pass, with source controller changed after activation, and assert Blast Cone/watcher attribution and the Standard=None control. The source path uses captured `resolving.controller` as requested; the existing test does not establish the real runner/request boundary.

## Test inventory reconciliation

Read-only test-function inventory exactly reproduces baseline4708 → candidate4707: 37 existing ignored tests enabled, zero active tests newly ignored, 44 added/renamed names and 45 removed/renamed names. Every exact saved regression name listed in triggers-current-port-review.md exists and is active, including the original additional commit4 regressions. The removed “today the engine hears nothing” assertions are obsolete and should not be restored merely to preserve a count; their positive/negative replacements are appropriate.

One meaningful accidental removal is `engine/statics::a_gears_granted_level_aura_and_rule_static_are_read_off_the_wearer` plus RELIC. Current e9074603 production restores attached Level, plain static and wearer-sourced aura behavior, so retain this independent regression unchanged. Root is restoring it separately; its restoration is not yet part of reviewed6820e6dd. Full raw inventory is saved in /tmp/agni-tests-{base,triggers,saved}.txt and /tmp/agni-trigger-test-changes.txt.

## Practical real Granted + Lent replay corpus

Use one small registered-card HostSession sequence in `net/tests/riftbound_turns.rs`, then reuse `assert_plugin_replay_parity` and the Damage cold-suffix pattern with two checkpoints. Setup remains normal Deal/roll/start entries. Add a test-only optional battlefield-name parameter to the M9 setup (defaults unchanged), because current `open_m9_game` hardcodes Crossroads/Sanctum: use Gardens of Becoming at BF1, ordinary BF2. Seat0 begins with Vi (plain registered/fallback unit fixture) and Warmog's Armor in base, two Body runes in pool; both seats retain nonempty hands so pending abilities receive normal priority. The standard helper channels two additional Mind runes on start; assert the actual four-rune starting pool.

1. Activate Warmog's printed Equip targeting Vi. Its printed equip cost is 0 energy plus one Body rune recycled. Assert exactly one Body rune leaves pool for rune deck, no energy-exhaust payment, and attachment/might+1. March Vi from base into empty Gardens through the actual Move admission; settle showdown normally. Conquer should produce `Granted{holder:Vi,lender:Warmog,index:GRANTED}` before priority completes. Save authoritative engine snapshot at that nonempty chain; record exact suffix index. Cold native and hardened engine+plugin instances restore and finish the real suffix. Assert buff is on Vi, not gear, and chain drains.
2. End turns normally until Vi readies on seat0's next turn (hold scoring may occur: record XP after beginning rather than hardcode total). Discover the Gardens “gain 1 XP” activation in the actual affordances; activate Vi. Assert Vi exhausts, Gardens does not pay/exhaust, rune state is unchanged, and chain contains `Lent{holder:Vi,lender:Gardens,index:GRANTED}` with controller0. Save a second authoritative snapshot before final passes. Fresh native/hardened restore completes the same suffix and adds exactly one XP to the recorded baseline.

Compare every verdict/entry, authoritative snapshot, both views and final plugin bytes. Require checkpoints actually contain the new variants; a quiet final-state roundtrip is insufficient. This combined corpus proves real declaration/payment/serialization/resolution for both kinds; it does not replace the separate changed-holder/departed-lender reload tests or prove A4a incarnation identity. No direct GameBlob trigger/holder/attachment/XP injection and no test script substituted for registered text.
