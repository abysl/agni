# Economy recovery independent review

Reviewed economy `7ca6bfb7` against integration `c1ac1817`, using common recovery base `1b517611` for the production delta and saved economy `1c790ef1` to distinguish merge mistakes from prior lane behavior. Read-only source review; no builds/tests run. Luna is already repairing four stale v10 expected-byte fixtures; those expected-version failures are not reported below as production bugs. Root has been notified of these findings for corrective work before integration.

## Must repair before integration

1. **P2 — owned Repeat grants no longer reach the play prompt.** `engine/play.rs:327` and `engine/prompts.rs:395` now call `cost::repeat_of`; that helper (`engine/cost.rs:338`) checks printed Repeat and static lenders but omits `Ctx::granted_cost`. `repeat_of_item` still includes the A9 owned row and is used for pricing. A spell with only an owned Repeat grant is therefore charged correctly if its slot is manufactured, but is never offered that slot through normal play. Unify the selection accessor, preserving the existing first-cost precedence pending A10. Add a serialize/reload → normal play → offered/paid Repeat regression using only an owned grant.

2. **P2 — repeated modal execution slices unequal target groups incorrectly.** `engine/chain.rs:163` computes one `per_execution = total_specs / executions`, although `targets::specs_of_execution` can return different lengths per selected mode. Disposal Order has one Recycle spec versus zero Draw specs; a promised Repeat selecting each mode yields one total spec divided by two, dropping the Recycle targets. Compute boundaries from each execution's actual mode/specs and recorded counts. Separately, `targets::group_start:101` still returns only the first execution's target count for all later executions, so a third execution's relative-target filters anchor in the second execution. Cover both unequal mode-spec counts and third-execution relative targets after reconstruction.

3. **P2 — the third supported execution never receives its own mode choice.** `engine/play.rs:608` retains `if repeated { 2 } else { 1 }`, while paid printed plus promised Repeat now gives three executions. Rocket Barrage can pay both, but only gets two mode choices; later target-spec fallback silently inherits the first mode instead. Enumerate `1 + item.repeats()` consistently. Test all three independently chosen modes through the real pending-item prompt sequence. This is integration of the already-supported two paid instances, not A10's broader arbitrary-instance work.

4. **P2 — additional-cost affordability is evaluated after pool/discount application.** `engine/play.rs:363` retains `cost::of_item(...).plus(additional)` and generic `pay::affordable`. In the new pipeline, `of_item` already applies discounts and records pool spending; `pay::from_pool:341` refuses another pass whenever `cost.pooled` is nonempty. Example: printed energy 1, additional energy 2, banked energy 3, no ready runes. The base consumes 1 pool energy in its quote; the appended 2 cannot use the remaining banked energy, so the affordable optional cost is not offered. A discount exceeding the printed cost has the analogous premature saturation problem. Quote a hypothetical item with the actual optional slot enabled through the complete pricing pipeline, using `Paying::Item`. Audit the retained generic affordability call in the Accelerate stage too: context-dependent Add sources should see the accelerated item. Test additional cost covered by remaining pool, by excess discount, and by an item-qualified source.

5. **P2 — legacy v7 discount is discarded by production migration.** `state.rs:688` constructs a permanent Any-card promise for the old `next_discount`, then the v7 match arm returns `Vec::new()` rather than that promise. The integration reader preserved this field for v7. Both independent v7 fixtures contain nonzero pairs; the fixture repair must preserve their discount semantics rather than encode the loss as the new expected output. Assert the owned migrated promise and exact canonical v11 bytes for both supported v7 seat shapes.

6. **P3 — unresolved documentation conflicts fail the diff gate.** `git diff --check 1b517611..7ca6bfb7` reports leftover conflict markers in `wiki/design/rules-engine.md` around lines 3966, 4241, 4312 and 4431 (32 marker diagnostics). Production Rust has no leftover markers in the inspected scan. Resolve the document and rerun the diff check before marking that gate passed.

## Preserved behavior and compatibility limits

A9's owned `CostedGrant`, symbolic power encoding, `of_grant`, and runtime Flow accessor survive; no global cost interner, leak, or general reverse converter was restored. The v11 writer uses an explicit fourteen-field seat row and retains the statics/A9 thirteen-field card row. It keeps PlayLock/readiness/champion/gear counters/pool independently. Nonzero v9/v10 statics discounts migrate to permanent play-only promises; matching discount indices are spent in descending deduplicated order. Ordinary statics v10 chain/pending mode slots shift from 6 to 7 to make room for promised Repeat.

The reader's special boolean-lock **seat** branch does not establish whole saved economy-v10 compatibility: saved economy had eleven-field cards, while this reader expects thirteen-field v10 cards; saved economy already used promised Repeat slot 6, while the blanket old-item migration shifts it as a mode. If the intended compatibility promise is the previously accepted statics v7/v9/v10 formats, state that scope explicitly. If saved economy blobs must load, require unambiguous whole-blob dispatch and fixtures, not only the isolated seat-row test. This is a support-scope decision, separate from the definite v7 regression above.

Known A10 first-cost/multiple-Repeat limitations and A6 promise-index saturation remain prior lane debt, not newly discovered merge regressions. Synchronous payment kills and replacement continuation issues remain assigned to A5/D1/A11. No new generic framework/spirit boundary violation was found. Later integration must preserve root's newer review/plan files absent from this worker branch rather than treat a two-tip diff as deletions to apply.

## Validation required for the repaired tip

Reported worker results are 4,412 library tests passed, 256 ignored, clippy/wasm checks, and existing native/hardened parity. They do not override the concrete source defects above. The four legacy expected-byte fixture repairs are in progress and must pass alongside the production v7 correction.

The hardened corpus in `net/tests/riftbound_turns.rs:461` still consists of target prompt, mulligan, and Hidden reveal; A9 adds costed-Flow decoding/EndTurn history parity. It has no Temporal Portal/Academy promised-Repeat or banked-pool continuation. Add one real promised-Repeat plus pool payment replay, reconstruct between the optional prompt and payment, and compare effects/state bytes/views natively and in hardened wasm. Existing same-spec three-execution assertions do not cover modal boundaries or relative targets.

After corrective regressions, run complete turns (including integration fixture tests), warnings-denied clippy, scoped formatting, diff/conflict checks, fresh wasm, and the expanded parity corpus. This review intentionally does not claim a full exhaustive review of all recovered card rules.
