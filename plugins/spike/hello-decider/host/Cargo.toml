[workspace]

[package]
name = "hello-decider-host"
version = "0.1.0"
edition = "2021"

[dependencies]
wasmtime = { version = "48", default-features = false, features = [
  "cranelift",
  "runtime",
] }
