[package]
name = "agni-sim"
version = "0.1.0"
edition = "2021"

[dependencies]
agni-core = { path = "../core" }
ciborium = "0.2"
serde = { version = "1", features = ["derive"] }
serde_bytes = "0.11"

[lib]
name = "agni_sim"
